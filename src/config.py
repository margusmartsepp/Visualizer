# Flask Server Configuration
DEFAULT_FLASK_HOST = '127.0.0.1'  # Default Flask host (localhost)
DEFAULT_FLASK_PORT = 5000         # Default Flask port

# Screenshot Capture Configuration
INTERVAL_SECONDS = 3              # Interval between screenshots in seconds
